experiment_id: EXP-2026-0001
date: 2026-09-11
owner: RJ
title: Testing Exo7
projects:
  - ATLAS
objective: >
  Test whether Exo7 exonuclease treatment selectively degrades non-specifically
  bound ssDNA-encoder probes while sparing correctly hybridized padlock probes.
decision: >
  Whether Exo7 treatment is viable for reducing non-specific probe background;
  if it works in 3T3 cells, next step is validating in tissue for use in the
  pipeline to remove non-specific binding.
comparison: >
  2x2 design: probe type (linear-to-rRNA, representing non-specific binding,
  vs. circular/padlock-to-rRNA, representing specific binding) x Exo7
  treatment (+/-), in 3T3 cells.
informative_outcome: >
  Cytoplasmic intensity per cell. A positive result is Exo7 treatment
  substantially reducing linear-probe signal while circular-probe signal
  remains largely intact.

design:
  cell_line: 3T3
  replicate_strategy: >
    Technical replicates only (2 wells per condition, all from a single
    plating/fixation batch on one plate). Not a biological-replicate design;
    a consistent result across the 2 wells demonstrates measurement
    reproducibility, not biological reproducibility.
  variables_held_constant:
    - cell line, plating density, and plate (all 10 wells from one batch)
    - Exo7 incubation window (all wells incubated together, 3 h at 37C)
  known_interpretive_limitation: >
    The linear probe serves both as the non-specific-binding test arm and as
    the only positive control for Exo7 enzymatic activity. A null result
    (no signal drop in the linear condition) will be ambiguous between
    "Exo7 was not active" and "exposed probe ends are not more susceptible
    than hypothesized" -- there is no independent construct in this design
    that isolates enzyme activity from the hypothesis under test.
  conditions:
    - id: C1
      label: linear probe, +Exo7
      probe: linear-to-rRNA
      readout_probe: YEH1
      exo7: "+"
      wells: 2
    - id: C2
      label: linear probe, -Exo7 (mock)
      probe: linear-to-rRNA
      readout_probe: YEH1
      exo7: "-"
      wells: 2
    - id: C3
      label: circular/padlock probe, +Exo7
      probe: circular-to-rRNA
      readout_probe: YEH2
      exo7: "+"
      wells: 2
    - id: C4
      label: circular/padlock probe, -Exo7 (mock)
      probe: circular-to-rRNA
      readout_probe: YEH2
      exo7: "-"
      wells: 2
    - id: C5
      label: no-encoder background
      probe: none
      readout_probe: YEH1
      exo7: "- (mock)"
      wells: 1
      purpose: baseline for YEH1 readout-probe non-specific binding without a target-specific encoder
    - id: C6
      label: no-probe background (autofluorescence)
      probe: none
      readout_probe: none
      exo7: "- (mock)"
      wells: 1
      purpose: >
        Redefined from the original "no-readout" concept: neither encoding
        nor readout probe is applied. Measures inherent cell/plate
        autofluorescence rather than testing whether the encoding probe
        itself is autofluorescent without a readout probe attached.
  total_wells: 10
  physical_well_assignment: not yet made; to be recorded at plating time

protocols:
  - protocol_id: pdl-coating-cell-plating-fixation
    version: "0.1"
    path: ../../../protocols/pdl-coating-cell-plating-fixation/protocol.md
    role: dependency (completed before hybridization)
    run_parameters:
      well_count: 10
      pdl_volume_per_well_ul: 25
      master_mix_overage_percent: 10
      measured_total_cells_per_ul: TBD (measured day-of; run calculations.py with the measured value)
      deviation: >
        The protocol's input is a viability-gated count
        (measured_viable_cells_per_ul); this run counts and targets all
        cells (total count), not only viable ones. Target is therefore
        10,000 total cells/well rather than 10,000 viable cells/well.
  - protocol_id: encoding-hybridization-imaging
    version: "0.2"
    path: ../../../protocols/encoding-hybridization-imaging/protocol.md
    role: primary protocol for encoding-probe hybridization, readout-probe hybridization, and imaging
    run_parameters:
      well_count: 10
      probe_final_concentration_nm: 1  # fixed by protocol
      probe_stock_concentration: 100  # nM, confirmed (both linear and circular encoding probes)
      readout_probe_identity: "YEH1 for linear-probe conditions (C1, C2, C5); YEH2 for circular-probe conditions (C3, C4); none for C6"
      readout_probe_stock_concentration: 100  # nM, confirmed (both YEH1 and YEH2)
      readout_probe_final_concentration: 5  # nM, confirmed
      readout_hybridization_volume_per_well_ul: 50  # confirmed
      readout_hybridization_duration: 30 min  # confirmed
      master_mix_overage_percent: 10
      post_encoding_hybridization_wash_conditions: "2 washes x 10 min each; buffer and temperature assumed TBST at room temperature (consistent with rest of protocol), not explicitly confirmed"
      post_readout_hybridization_wash_conditions: "2 washes x 10 min each; buffer and temperature assumed TBST at room temperature, not explicitly confirmed"
      imaging_configuration: "Deliberately deferred to the operator at time of imaging (exposure and acquisition settings set on the microscope); not fixed in advance by design"
  - protocol_id: exo7-digestion (ad hoc, not a canonical protocol)
    role: run-specific step inserted after post-encoding-hybridization wash, before readout-probe hybridization
    run_parameters:
      buffer: TBE
      enzyme_units_per_well: 10
      reaction_volume_per_well_ul: 50
      incubation_temperature_c: 37
      incubation_duration_h: 3
      applied_to: wells in conditions C1 and C3 (the "+Exo7" conditions)
      mock_treatment: >
        Wells in conditions C2, C4, C5, C6 receive the same 50 uL TBE buffer
        with no enzyme, incubated together with the +Exo7 wells for the same
        3 h/37C window (all 10 wells are on one plate and are incubated
        together).

calculations:
  pdl_cell_pfa: >
    Use protocols/pdl-coating-cell-plating-fixation/calculations.py with
    well_count=10, pdl_volume_per_well_ul=25, overage_percent=10, and the
    measured_viable_cells_per_ul recorded at plating time. Not yet run --
    depends on a same-day measurement.
  exo7_master_mix: >
    4 wells (C1, C3) require the 10 U/50 uL TBE Exo7 reaction; 6 wells (C2,
    C4, C5, C6) require the 50 uL TBE-only mock. Apply 10% overage to each
    master mix. Not yet computed in absolute reagent volumes -- straightforward
    once confirmed, held here pending manifest review.
  hybridization_mixes:
    encoding_probe_mixes: >
      Resolved. Encoding-probe stock = 100 nM, final = 1 nM, 40 uL/well
      (protocol's general nM formula), 10% overage. Three separate master
      mixes needed (encoding probe identity differs by condition):
      linear mix for C1+C2 (4 wells): 1.76 uL linear probe stock, 70.4 uL
      50% formamide, 103.84 uL TBST;
      circular mix for C3+C4 (4 wells): 1.76 uL circular probe stock,
      70.4 uL 50% formamide, 103.84 uL TBST;
      mock (no-probe) mix for C5+C6 (2 wells): 0 uL probe, 35.2 uL 50%
      formamide, 52.8 uL TBST.
      Per-well volumes: 0.4 uL probe stock (linear/circular mixes only),
      16 uL 50% formamide, 23.6 uL TBST (24 uL TBST in the mock mix) = 40 uL total.
    readout_probe_mixes: >
      Resolved. Readout stock = 100 nM, final = 5 nM, 10% formamide final
      (from 50% formamide stock), 50 uL/well, 30 min hybridization at room
      temperature, 10% overage. Two master mixes needed (readout probe
      differs by condition): YEH1 mix for C1+C2+C5 (5 wells): 13.75 uL YEH1
      stock, 55.0 uL 50% formamide, 206.25 uL TBST; YEH2 mix for C3+C4
      (4 wells): 11.0 uL YEH2 stock, 44.0 uL 50% formamide, 165.0 uL TBST.
      C6 receives no readout probe and is excluded from both mixes.
      Per-well volumes: 2.5 uL probe stock, 10 uL 50% formamide, 37.5 uL
      TBST = 50 uL total.

execution:
  ordered_plan:
    - step: 1
      description: >
        Plate and fix 3T3 cells per pdl-coating-cell-plating-fixation (10 wells,
        25 uL PDL, 10% overage; measure viable cell concentration day-of).
    - step: 2
      description: >
        Encoding-probe hybridization per encoding-hybridization-imaging steps
        1-2 (pre-hybridization washes, then overnight hybridization at 37C,
        20% formamide, 1 nM final) -- distinct encoding probe (linear or
        circular or none) per condition C1-C6.
    - step: 3
      description: "Post-encoding-hybridization wash (step 3): 2 washes x 10 min each, TBST at room temperature assumed."
    - step: 4
      description: >
        Exo7 / mock incubation -- add 50 uL reaction (10 U Exo7 in TBE for
        C1/C3; TBE only for C2/C4/C5/C6) to each well; incubate all 10 wells
        together, 3 h at 37C.
    - step: 5
      description: "Wash: 2 washes x 10 min each, TBST at room temperature assumed."
    - step: 6
      description: >
        Readout-probe hybridization per encoding-hybridization-imaging step 4
        (room temperature, 10% formamide, TBST, 50 uL/well, 30 min; YEH1 for
        C1/C2/C5, YEH2 for C3/C4) -- omitted for condition C6 (no-probe
        autofluorescence control).
    - step: 7
      description: "Post-readout-hybridization wash (step 5): 2 washes x 10 min each, TBST at room temperature assumed."
    - step: 8
      description: >
        Imaging per encoding-hybridization-imaging step 6 (microscope/
        acquisition settings TBD).

analysis:
  primary_measurement: cytoplasmic intensity per cell
  segmentation_normalization: >
    Deferred to appropriate available image-analysis skills at analysis time
    (cell segmentation, background subtraction, per-cell vs per-well
    aggregation); no specific pipeline fixed in advance.
  planned_comparisons: >
    Within each probe type, +Exo7 vs -Exo7 cytoplasmic intensity (C1 vs C2,
    C3 vs C4); across probe types, relative Exo7-induced signal change
    (linear vs circular). C5 and C6 used as background/validity references,
    not part of the primary comparison.
  qc_exclusions: >
    Deferred to appropriate available image-analysis skills at analysis time;
    no specific exclusion rule fixed in advance.
  expected_outputs: per-cell intensity table and images per well; summary
    comparison figure

preflight:
  status: blocking issue unresolved -- surfaced while writing the bench sheet
  blockers:
    - >
      Attachment/growth period between plating and fixation is not specified
      in this manifest or in pdl-coating-cell-plating-fixation (the protocol
      defers it as experiment-specific). No value has been decided, so the
      run is not executable as written. Surfaced by bench-sheet generation,
      not by section-by-section preflight.
  warnings:
    - >
      Whether C6 (no-readout well) should receive 50 uL of probe-free buffer
      during the readout-hybridization step -- matching handling the way the
      Exo7 mock does -- is not specified. Unmatched handling is a minor
      confound for the autofluorescence baseline. Surfaced by bench-sheet
      generation.
    - >
      C5 (no-encoder background) uses YEH1 only. YEH2's non-specific-binding
      background is therefore untested by this design -- if YEH1 and YEH2
      differ in non-specific behavior, the circular-probe condition (C3/C4,
      which uses YEH2) has no matched background reference. Accepted as-is
      per instruction; flagged for awareness, not blocking.
    - >
      C6 was redefined from "encoder present, no readout" (tests whether the
      encoding probe itself is autofluorescent) to "no probe at all" (tests
      general cell/plate autofluorescence). These are different controls;
      the original encoder-autofluorescence question is no longer directly
      addressed by any well in this design.
    - technical-replicate-only design (n=2, single plating batch); no biological
      replication in this pilot
    - linear probe serves as both test arm and sole positive control for Exo7
      activity (see design.known_interpretive_limitation)
    - post-encoding-hybridization and post-readout-hybridization wash buffer/temperature
      assumed as TBST at room temperature (consistent with the rest of the protocol,
      count and duration confirmed at 2x10 min) but not explicitly confirmed
    - imaging/acquisition configuration deliberately deferred to the operator at
      time of imaging; not fixed in advance by design, not a gap
    - analysis segmentation/normalization/QC deliberately deferred to appropriate
      image-analysis skills at analysis time; not fixed in advance by design
    - >
      Cell counting deviates from the pdl-coating-cell-plating-fixation
      protocol's viable-cell input -- total cell count used instead of
      viability-gated count (see protocols[0].run_parameters.deviation).

status: DESIGN_COMPLETE
